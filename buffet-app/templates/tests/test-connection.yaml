apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "buffet-app.fullname" . }}-test-connection"
  labels:
    {{- include "buffet-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "buffet-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
