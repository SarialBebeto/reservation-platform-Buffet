apiVersion: v1
kind: Service
metadata:
  name: {{ include "buffet-app.fullname" . }}
  labels:
    {{- include "buffet-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "buffet-app.selectorLabels" . | nindent 4 }}
